
// w a p to write print 0 to till no and 05 times

#include <stdio.h>
void main()
{
    int n, i;

    int j, k, l, m, p;

    j = 5;

    j = k = l = m = p;

    printf("Enter a No");
    scanf("%d", &n);

    // printf("How times loop wants:");
    // scanf("%d", &j);

    printf("Series is as :");
    for (i = 0; i <= n; i++)
    {

        printf("%d %d %d\n", i,i,i);
        }
}